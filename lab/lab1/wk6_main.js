var

var state = {
  count: 0;
  data: undefined;
};


var nextfruit = function(){
  var limit = state.data.length - 1;
  if (count + 1 > limit){
    state.count = 0;
  } else {
    state.count += 1;
  }
  return state.data;
};

var preFruit = function(){
  if (state.count - 1 < 0){
    state.count += 1;


  }

}

var getHexColor = function(){
    if (fruitObject.color === "yellow"){
      return "";
    }
    else if (){
      return "";
    }
    else if (){
      return "";
    }
};
$('body').css('background-color',getHexColor());





$.ajax('').done(function(){
  var pasedData = JSON.parse(data);
  state.data = xxx;
  $('#button').plotting(data);

  var setStyle =



/* =====================
//notes: the sequence of button clicks;
//getting information other than color;
//
//
===================== */

}
